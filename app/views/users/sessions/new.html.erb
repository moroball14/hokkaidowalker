<div class="login-wrapper">
  <div class="login-wrapper__form">
    <%= link_to "北海道Walker", root_path, class: "brand-logo header__title" %>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.label :email, "メールアドレス", {class: 'field__label'} %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'field__form' %>
      </div>

      <div class="field">
        <%= f.label :password, "パスワード", {class: 'field__label'} %><br />
        <%= f.password_field :password, autocomplete: "current-password", class: 'field__form' %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me, {class: 'field__label'} %>
        </div>
      <% end %>

      <div class="actions field">
        <%= f.submit "ログイン", class: "search-form__element search-form__submit" %>
      </div>
    <% end %>
    <div class="field">
      <%= render "users/shared/links" %>
    </div>
  </div>
</div>
